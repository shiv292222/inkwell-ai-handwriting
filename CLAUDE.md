# Claude project context

Read and follow `AGENTS.md`. For handwritten-note requests, prefer the local MCP tool `create_handwritten_notes` or the Python `inkwell` CLI. Produce a real PDF or DOCX unless the user asks for PNG pages. Keep all note content on-device and preserve the responsible-use limits in `SECURITY.md`.
