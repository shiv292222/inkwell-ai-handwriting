# Inkwell coding instructions

Follow `AGENTS.md`. Keep the production renderer local-first, deterministic, and shared across CLI and MCP. Measure layout with the loaded font and retain collision safety. Do not add remote fonts, telemetry, uploads, signature imitation, or handwriting-cloning features.
